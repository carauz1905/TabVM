# This file is intentionally empty in source control.
# scripts/dev-start.ps1 generates a per-run session token and exports it for Vite.
# For manual development, copy .env.example to .env.local and set a real token.
